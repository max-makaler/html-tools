version: '3.8'

services:
  app:
    build: .
    container_name: html-tools-app
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data